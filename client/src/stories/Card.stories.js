import React from "react";
import Card from "./../components/Card";

export default {
  title: "Site/Card",
  component: Card,
};

const Template = () => <Card />;

export const GameCard = Template.bind({});
