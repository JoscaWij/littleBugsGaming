import React from "react";
import styled from "@emotion/styled";

const Header = () => {
  return <HeaderWrapper>Welcome</HeaderWrapper>;
};

export default Header;

const HeaderWrapper = styled.div`
  background-color: grey;
  width: 100%;
`;
